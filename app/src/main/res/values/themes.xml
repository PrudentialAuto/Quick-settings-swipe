<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme. The Compose UI supplies its own Material 3 colours; this
         theme just provides a sane, edge-to-edge friendly platform shell. -->
    <style name="Theme.PanelSwipe" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
